import React from "react";
import ReactDOM from "react-dom";
import Weather from './API/weather.jsx'

ReactDOM.render(
    <React.StrictMode>
        <Weather/>
    </React.StrictMode>,
    document.getElementById("root"),
    );

